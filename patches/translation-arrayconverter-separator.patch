diff --git a/vendor/symfony/translation/Util/ArrayConverter.php b/vendor/symfony/translation/Util/ArrayConverter.php
index 2fc666d..9664f2a 100644
--- a/vendor/symfony/translation/Util/ArrayConverter.php
+++ b/vendor/symfony/translation/Util/ArrayConverter.php
@@ -14,8 +14,8 @@ namespace Symfony\Component\Translation\Util;
 /**
  * ArrayConverter generates tree like structure from a message catalogue.
  * e.g. this
- *   'foo.bar1' => 'test1',
- *   'foo.bar2' => 'test2'
+ *   'foo bar1' => 'test1',
+ *   'foo bar2' => 'test2'
  * converts to follows:
  *   foo:
  *     bar1: test1
@@ -25,9 +25,12 @@ namespace Symfony\Component\Translation\Util;
  */
 class ArrayConverter
 {
+    // separador usado para quebrar as chaves (por padrão: espaço)
+    private const SEPARATOR = ' ';
+
     /**
      * Converts linear messages array to tree-like array.
-     * For example: ['foo.bar' => 'value'] will be converted to ['foo' => ['bar' => 'value']].
+     * For example: ['foo bar' => 'value'] will be converted to ['foo' => ['bar' => 'value']].
      *
      * @param array $messages Linear messages array
      */
@@ -55,12 +58,12 @@ class ArrayConverter
             if (isset($elem[$part]) && \is_string($elem[$part])) {
                 /* Process next case:
                  *    'foo': 'test1',
-                 *    'foo.bar': 'test2'
+                 *    'foo bar': 'test2'
                  *
                  * $tree['foo'] was string before we found array {bar: test2}.
-                 *  Treat new element as string too, e.g. add $tree['foo.bar'] = 'test2';
+                 *  Treat new element as string too, e.g. add $tree['foo bar'] = 'test2';
                  */
-                $elem = &$elem[implode('.', \array_slice($parts, $i))];
+                $elem = &$elem[implode(self::SEPARATOR, \array_slice($parts, $i))];
                 break;
             }
 
@@ -70,12 +73,12 @@ class ArrayConverter
 
         if ($elem && \is_array($elem) && $parentOfElem) {
             /* Process next case:
-             *    'foo.bar': 'test1'
+             *    'foo bar': 'test1'
              *    'foo': 'test2'
              *
              * $tree['foo'] was array = {bar: 'test1'} before we found string constant `foo`.
              * Cancel treating $tree['foo'] as array and cancel back it expansion,
-             *  e.g. make it $tree['foo.bar'] = 'test1' again.
+             *  e.g. make it $tree['foo bar'] = 'test1' again.
              */
             self::cancelExpand($parentOfElem, $part, $elem);
         }
@@ -85,7 +88,7 @@ class ArrayConverter
 
     private static function cancelExpand(array &$tree, string $prefix, array $node): void
     {
-        $prefix .= '.';
+        $prefix .= self::SEPARATOR;
 
         foreach ($node as $id => $value) {
             if (\is_string($value)) {
@@ -101,7 +104,8 @@ class ArrayConverter
      */
     private static function getKeyParts(string $key): array
     {
-        $parts = explode('.', $key);
+        // explode usando o separador (mantém partes vazias como o explode original fazia)
+        $parts = explode(self::SEPARATOR, $key);
         $partsCount = \count($parts);
 
         $result = [];
@@ -109,13 +113,13 @@ class ArrayConverter
 
         foreach ($parts as $index => $part) {
             if (0 === $index && '' === $part) {
-                $buffer = '.';
+                $buffer = self::SEPARATOR;
 
                 continue;
             }
 
             if ($index === $partsCount - 1 && '' === $part) {
-                $buffer .= '.';
+                $buffer .= self::SEPARATOR;
                 $result[] = $buffer;
 
                 continue;
